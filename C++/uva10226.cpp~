#include<iostream>
#include<cstdio>
#include<cstring>
#include<map>
#include<vector>
#include<algorithm>

#define maxx 10005

using namespace std;

struct node
{
	char name[35];
	int num;
}tree[maxx];

int main()
{
	int test;
	vector<string> v;
	char s[35];
	scanf("%d",&test);
	gets(s);
	gets(s);
	while(test--){
		int num=0;
		while(gets(s)){
			if(s[0]!='\0'){
			/*	if(qq[s]==0){
					qq[s]=num++;
				}
			*/
			//printf("%s\n",s);
			v.push_back(s);
			}
			else	break;
		}
		sort(v.begin(),v.end());
		for(int i=0;i<v.size();i++)
		cout<<v[i]<<endl;
		v.clear();
		if(test)	printf("\n");
	}
	return 0;
}
